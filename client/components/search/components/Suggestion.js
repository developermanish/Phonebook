import React, { useState } from 'react';

const SuggestionList = () => {
    const [activeSuggestion, setActiveSuggestion] = useState(0);
    const [filteredSuggetion, setFilteredSuggetion] = useState([]);
    return (
        <div>

        </div>
    )
}
export default SuggestionList;